using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Ejercicio6_1 : MonoBehaviour
{

    //4.1

    //(X && Y) || ( X && Z) == T

    //(X || !Y) && (!X || Z) == T
    
    //X || Y && Z == T

    //!(X || Y) && Z == F

    //X || Y || X && !Z && !Y == T

    //!X || !Y || Z && X && !Y == T

    //4.2

    //W || Y && X && W || Z == F

    //X && !Y && !X || !W && Y == T

    //!(W||!Y) && X || Z == T

    // X && Y && W || Z || X == T

    //Y || !(Y || Z && W) == T

    //!X && Y && (!Z || !X) == F


   
}
